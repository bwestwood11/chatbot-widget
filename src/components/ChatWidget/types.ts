
export type WidgetProps = {
  apiKey: string;
  themeColor?: string;
  textColor?: string;
};

export type TChatBoxDetails = {
  chatBotName: string;
  chatBotDescription: string;
  colorScheme: string;
  welcomeMessage: string;
  apiKey: string;
  logoUrl: string;
  textColor: string;
};